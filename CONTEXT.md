# 注音冒險島 (Bopomofo Adventure)

一套供台灣幼兒園大班與國小低年級學童使用的注音符號學習系統。

## Language

**注音標注 (Bopomofo Annotation)**:
指 `BopomofoRuby` 元件中，緊貼漢字旁的聲母/介音/韻母/聲調符號（`bpmf`/`tone` 樣式部分）。與「漢字」本身的字級是獨立控制的兩個尺寸維度。
_Avoid_: 注音文字（過於籠統，未區分是指注音標注本身還是其他獨立出現的注音符號說明文字）

**注音字級設定 (Bopomofo Annotation Scale Setting)**:
一個持久化（`localStorage`）、可由使用者於設定中調整的全域設定，只控制「注音標注」的字級大小，不影響「漢字」字級，也獨立於各呼叫端既有的 `size`（`sm`/`md`/`lg`/`xl`）結構性版型選擇。分為「標準」「大」「特大」三級，預設為「大」。每往上一級，呼叫端就相對地套用既有 `size` 梯子中下一格的注音字級；已在梯子頂端（`xl`）的呼叫端封頂、不再變大。詳見 [ADR-0001](docs/adr/0001-bopomofo-annotation-scale-setting.md)。
_Avoid_: 字體大小設定（範圍過廣，實際只作用於注音標注）

**字卡正面 (Flashcard Front)**：
3D 字卡（`FlashcardDrillView`）翻轉卡片未翻面時看到的那一側。永久不顯示漢字，只顯示放大到填滿原本漢字視覺區塊的注音標注，且不提供點擊發音——是刻意設計的「先看注音猜字」關卡，不是可切換的模式。
_Avoid_: 題目（過於籠統，且暗示這是一種可開關的測驗模式，但實際上是永久的預設行為）

**字卡背面 (Flashcard Back)**：
3D 字卡翻面後看到的一側，顯示 Emoji、完整漢字、字義與附注音例句，是「字卡正面」猜字後的解答揭曉畫面。
_Avoid_: 答案（可用但不夠精確，背面除了解答還有例句等延伸學習內容）
